# Resume <-> JD Fit Analyzer — Python Dependencies
# Generated from pip freeze (Python 3.14 environment)
# Install: pip install -r requirements.txt
#
# Note: torch (CPU-only) is heavy (~2GB). If you need GPU support,
#       install manually: pip install torch --index-url https://download.pytorch.org/whl/cu121

# ── Core API framework ──────────────────────────────────────
fastapi==0.141.1
uvicorn==0.52.4
starlette==1.6.0
python-multipart==0.0.32

# ── LLM & AI ────────────────────────────────────────────────
google-genai==2.19.0
instructor==1.15.4
openai==2.54.0               # required by instructor

# ── Embedding model (SkillNormalizer) ───────────────────────
sentence-transformers==6.0.0
torch==2.13.0                # CPU-only; ~2GB download
transformers==5.15.1
tokenizers==0.22.2
safetensors==0.8.0
huggingface_hub==1.28.0

# ── Data processing ─────────────────────────────────────────
numpy==2.5.2
pandas==3.0.5
scikit-learn==1.9.0
scipy==1.18.1

# ── File parsing (PDF / DOCX) ───────────────────────────────
pdfplumber==0.11.10
pypdfium2==5.13.0
pdfminer.six==20260107
python-docx==1.2.0

# ── HTTP / networking ────────────────────────────────────────
httpx==0.28.1
httpcore==1.0.9
requests==2.34.2
aiohttp==3.14.3
anyio==4.14.2

# ── Schema / validation ─────────────────────────────────────
pydantic==2.13.4
pydantic_core==2.46.4
jsonref==1.1.0               # ⚠️ Required by instructor for schema resolution
                              # Without this: 500 error on first API call

# ── Retry / resilience ──────────────────────────────────────
tenacity==9.1.4

# ── Utilities ───────────────────────────────────────────────
python-dotenv==1.2.3
tqdm==4.70.0
rich==14.3.4
click==8.4.2

# ── Auth ────────────────────────────────────────────────────
google-auth==2.57.0
cryptography==50.0.0

# ── Other transitive dependencies (pinned for reproducibility) ─
annotated-types==0.8.0
certifi==2026.7.22
charset-normalizer==3.5.1
colorama==0.4.6
distro==1.9.0
docstring_parser==0.18.0
filelock==3.32.4
frozenlist==1.8.0
fsspec==2026.7.0
h11==0.16.0
idna==3.19
Jinja2==3.1.6
jiter==0.14.0
joblib==1.5.3
lxml==6.1.2
MarkupSafe==3.0.3
mpmath==1.3.0
multidict==6.7.1
narwhals==2.25.0
networkx==3.6.1
packaging==26.3
pillow==12.3.0
propcache==0.5.2
pyasn1==0.6.4
pyasn1_modules==0.4.2
python-dateutil==2.9.0.post0
PyYAML==6.0.3
regex==2026.7.19
setuptools==84.0.0
six==1.17.0
sniffio==1.3.1
sympy==1.14.0
threadpoolctl==3.6.0
typing_extensions==4.16.0
typing-inspection==0.4.4
tzdata==2026.3
urllib3==2.7.0
websockets==16.1.1
yarl==1.24.5
